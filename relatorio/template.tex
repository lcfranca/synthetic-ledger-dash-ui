\documentclass[$for(classoption)$$classoption$$sep$,$endfor$]{$documentclass$}
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{lmodern}
\else
  \usepackage{fontspec}
  \setmainfont{TeX Gyre Pagella}
  \setsansfont{TeX Gyre Heros}
  \setmonofont{Latin Modern Mono}
\fi
\usepackage[a4paper,left=3cm,top=3cm,right=2cm,bottom=2cm]{geometry}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{ragged2e}
\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{xurl}
\usepackage{bookmark}
\usepackage{enumitem}
\usepackage{pdflscape}
\usepackage{setspace}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\hypersetup{
  pdftitle={$title$},
  pdfauthor={$author$},
  pdfsubject={$document_scope$},
  hidelinks
}

\OnehalfSpacing
\setlength{\parindent}{1.25cm}
\setlength{\parskip}{0pt}
\setlist[itemize]{topsep=4pt,itemsep=2pt}
\setlist[enumerate]{topsep=4pt,itemsep=2pt}
\newcolumntype{L}[1]{>{\RaggedRight\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\Centering\arraybackslash}p{#1}}

$if(title)$
$if(subtitle)$
\titulo{$title$\\[0.3cm]\large $subtitle$}
$else$
\titulo{$title$}
$endif$
$endif$
$if(author)$
\autor{$author$}
$endif$
$if(institution)$
\instituicao{$institution$}
$endif$
$if(location)$
\local{$location$}
$endif$
$if(date)$
\data{$date$}
$endif$
$if(document_type)$
\tipotrabalho{$document_type$}
$endif$
$if(document_scope)$
\preambulo{$document_scope$}
$endif$

$for(header-includes)$
$header-includes$
$endfor$

\begin{document}
\selectlanguage{brazil}
\frenchspacing

\pretextual
$if(titlepage)$
\imprimircapa
\imprimirfolhaderosto*
$endif$

$if(abstract_pt)$
\begin{resumo}
$abstract_pt$

\vspace{\onelineskip}
\noindent
\textbf{Palavras-chave}: $keywords_pt$.
\end{resumo}
$endif$

$if(abstract_en)$
\begin{resumo}[Abstract]
\begin{otherlanguage*}{english}
$abstract_en$

\vspace{\onelineskip}
\noindent
\textbf{Keywords}: $keywords_en$.
\end{otherlanguage*}
\end{resumo}
$endif$

$if(scope_note)$
\chapter*{Nota de Escopo}
$scope_note$

$if(editorial_note)$
\section*{Critérios Editoriais}
$editorial_note$
$endif$
$endif$

$if(toc)$
\pdfbookmark[0]{\contentsname}{toc}
\tableofcontents*
\clearpage
$endif$

$if(lof)$
\listoffigures*
\clearpage
$endif$

$if(lot)$
\listoftables*
\clearpage
$endif$

\textual
$body$

\end{document}