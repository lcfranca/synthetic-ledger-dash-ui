apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: synthetic-ledger
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: synthetic-ledger/frontend:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: synthetic-ledger
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
